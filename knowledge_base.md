<!--
  OGGY's knowledge base.

  Everything you write in here gets folded into OGGY's system prompt on
  every turn (see core/knowledge.py + core/orchestrator.py), so OGGY
  always has this context without you repeating it in chat.

  - Plain markdown. No required schema - headings below are just a
    starting point, edit/delete/add freely.
  - Re-read from disk every turn, so just save the file - no restart.
  - Keep it factual and reasonably short (see OGGY_KNOWLEDGE_BASE_MAX_CHARS
    in .env if you need more room). This is context, not a chat log.
  - Don't put secrets here (API keys, passwords, tokens) - it goes
    straight into every LLM request, same as anything else in the
    system prompt.
-->

# About Me
- Name: Mahendiran
- Role / what I do: Developer
- Timezone:

# Current Work / Projects
- BIT_GUARD: Main active project located at `C:\Users\mahen\BIT_GUARD`

# Preferences
- Coding style / conventions:
- How I like answers formatted (e.g. concise, step-by-step, code-first):

# Standing Context
<!-- Anything else OGGY should just know: tech stack, recurring tools,
     ongoing goals, things you don't want to re-explain every time. -->
-
